# System Architecture - AI Growth Accelerator

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
│                     (React + TypeScript)                         │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ HTTP/REST API
                             │ (multipart/form-data)
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                     CUSTOM BACKEND API                           │
│                  (Express + TypeScript)                          │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │               Campaign Orchestrator                       │  │
│  │  • Request validation                                     │  │
│  │  • Async processing                                       │  │
│  │  • Status tracking                                        │  │
│  │  • Error handling                                         │  │
│  └────────────┬─────────────────────────────────────────────┘  │
│               │                                                  │
│  ┌────────────▼─────────────────────────────────────────────┐  │
│  │           CSV Processor Service                           │  │
│  │  • Parse CSV files                                        │  │
│  │  • Validate lead data                                     │  │
│  │  • Deduplicate by email                                   │  │
│  │  • Normalize field names                                  │  │
│  └────────────┬─────────────────────────────────────────────┘  │
│               │                                                  │
│  ┌────────────▼─────────────────────────────────────────────┐  │
│  │        Personalization Service                            │  │
│  │  • Batch processing (10 leads/batch)                      │  │
│  │  • Parallel AI calls                                      │  │
│  │  • Result aggregation                                     │  │
│  │  • KPI calculation                                        │  │
│  └──────────┬─────────────────────────┬─────────────────────┘  │
│             │                          │                         │
│  ┌──────────▼──────────┐    ┌─────────▼──────────┐             │
│  │  Perplexity Service │    │   Claude Service   │             │
│  │  • Company research │    │  • Personalization │             │
│  │  • Retry logic (3x) │    │  • JSON parsing    │             │
│  │  • Error handling   │    │  • Retry logic (3x)│             │
│  └──────────┬──────────┘    └─────────┬──────────┘             │
│             │                          │                         │
│  ┌──────────▼──────────────────────────▼──────────┐             │
│  │           Supabase Service                      │             │
│  │  • Campaign CRUD                                │             │
│  │  • Lead storage                                 │             │
│  │  • KPI updates                                  │             │
│  │  • Status tracking                              │             │
│  └─────────────────────────────────────────────────┘             │
└────────────────────────┬────────────────────────────────────────┘
                         │
        ┌────────────────┼────────────────┬───────────────┐
        │                │                │               │
┌───────▼────────┐ ┌────▼─────┐ ┌────────▼──────┐ ┌─────▼─────┐
│   Supabase     │ │Perplexity│ │  OpenRouter   │ │  Future   │
│   PostgreSQL   │ │    AI    │ │   (Claude)    │ │   APIs    │
│                │ │  (Sonar) │ │  (Sonnet 4)   │ │           │
└────────────────┘ └──────────┘ └───────────────┘ └───────────┘
```

## 🔄 Request Flow - Campaign Processing

```
1. USER UPLOADS CSV
   ↓
2. Frontend validates file
   ↓
3. Frontend calls agaBackend.processCampaign()
   ↓
4. POST /api/campaigns/process
   │
   ├─→ Campaign Orchestrator receives request
   ├─→ Validates input (all required fields present)
   ├─→ Updates status: "extracting"
   ├─→ Returns run_id immediately (202 Accepted)
   │
5. ASYNC PROCESSING BEGINS
   ↓
6. CSV Processor
   ├─→ Parse CSV with csv-parse
   ├─→ Normalize column names
   ├─→ Validate required fields (email, company)
   ├─→ Remove duplicates by email
   ├─→ Apply demo limit if needed (20 leads)
   ├─→ Returns array of validated leads
   │
7. Update status: "personalizing"
   ↓
8. Personalization Service
   │
   ├─→ Split leads into batches (10 leads each)
   │
   └─→ For each batch (parallel):
       │
       └─→ For each lead:
           │
           ├─→ Check if already exists in campaign_leads
           │   └─→ Skip if exists
           │
           ├─→ PERPLEXITY RESEARCH
           │   ├─→ Build research prompt
           │   ├─→ Call Perplexity API
           │   ├─→ Retry on failure (3x, 2s delay)
           │   └─→ Return research findings
           │
           ├─→ CLAUDE PERSONALIZATION
           │   ├─→ Build system prompt (task, guidelines, example)
           │   ├─→ Build user prompt (lead + research)
           │   ├─→ Call OpenRouter/Claude API
           │   ├─→ Parse JSON response
           │   ├─→ Retry on failure (3x, 2s delay)
           │   └─→ Extract personalized_sentence
           │
           ├─→ SAVE TO DATABASE
           │   ├─→ Append to campaign_leads.lead_data array
           │   ├─→ Deduplicate by email
           │   └─→ Update campaign.completed_count
           │
           └─→ UPDATE KPIs
               ├─→ num_personalized_leads += 1
               ├─→ hours_saved += 0.02
               └─→ money_saved += 0.04
   │
9. Update status: "completed" (or "failed" if errors)
   ↓
10. Frontend polls /api/campaigns/status/:runId
    │
    └─→ When complete, fetches results
        └─→ GET /api/campaigns/results/:campaignLeadsId
```

## 📁 Service Layer Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    SERVICE LAYER                             │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │     Campaign Orchestrator Service                  │    │
│  │  (campaign-orchestrator.ts)                        │    │
│  │                                                     │    │
│  │  • processCampaign()                               │    │
│  │  • extractCSVLeads()                               │    │
│  │  • getCampaignStatus()                             │    │
│  │  • getCampaignResults()                            │    │
│  │  • exportCampaignToCSV()                           │    │
│  │  • validateInput()                                 │    │
│  └──────────────────┬──────────────────────────────────┘    │
│                     │                                        │
│     ┌───────────────┼───────────────┐                       │
│     │               │               │                       │
│  ┌──▼──────────┐ ┌─▼──────────┐ ┌──▼──────────────┐        │
│  │   CSV       │ │Personaliza-│ │   Supabase      │        │
│  │ Processor   │ │   tion     │ │   Service       │        │
│  │  Service    │ │  Service   │ │                 │        │
│  │             │ │            │ │                 │        │
│  │ • extract   │ │ • personal-│ │ • getCampaign() │        │
│  │   Leads()   │ │   izeLead()│ │ • getCampaign-  │        │
│  │ • normalize │ │ • personal-│ │   Lead()        │        │
│  │   Data()    │ │   izeLeads│ │ • updateCampaign│        │
│  │ • remove    │ │   ()       │ │   Leads()       │        │
│  │   Duplicat- │ │ • validate │ │ • appendLead()  │        │
│  │   es()      │ │   Config() │ │ • checkLead-    │        │
│  │ • validate  │ │            │ │   Exists()      │        │
│  │   Lead()    │ │            │ │ • updateClient- │        │
│  │ • applyDemo │ │            │ │   Metrics()     │        │
│  │   Limit()   │ │            │ │ • updateRun-    │        │
│  │             │ │            │ │   Status()      │        │
│  └─────────────┘ └─────┬──────┘ └─────────────────┘        │
│                        │                                     │
│            ┌───────────┴───────────┐                        │
│            │                       │                        │
│     ┌──────▼──────┐       ┌───────▼────────┐               │
│     │ Perplexity  │       │    Claude      │               │
│     │   Service   │       │   Service      │               │
│     │             │       │                │               │
│     │ • research()│       │ • personalize()│               │
│     │ • buildUser │       │ • buildSystem- │               │
│     │   Prompt()  │       │   Prompt()     │               │
│     │             │       │ • buildUser-   │               │
│     │             │       │   Prompt()     │               │
│     └─────────────┘       └────────────────┘               │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## 🗄️ Database Schema & Relationships

```
┌─────────────────────────────────────────────────────────────┐
│                     SUPABASE DATABASE                        │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────────────────────────────────────────┐      │
│  │               campaigns                           │      │
│  ├──────────────────────────────────────────────────┤      │
│  │ id: uuid (PK)                                    │      │
│  │ name: string                                     │      │
│  │ user_id: uuid (FK → auth.users)                 │      │
│  │ completed_count: integer                         │      │
│  │ created_at: timestamp                            │      │
│  │ updated_at: timestamp                            │      │
│  └───────────────┬──────────────────────────────────┘      │
│                  │                                          │
│                  │ 1:1                                      │
│                  │                                          │
│  ┌───────────────▼──────────────────────────────────┐      │
│  │            campaign_leads                         │      │
│  ├──────────────────────────────────────────────────┤      │
│  │ id: uuid (PK)                                    │      │
│  │ campaign_id: uuid (FK → campaigns.id)            │      │
│  │ user_id: uuid (FK → auth.users)                 │      │
│  │ lead_data: jsonb[] ← ENRICHED LEADS HERE        │      │
│  │   [                                              │      │
│  │     {                                            │      │
│  │       first_name: string,                        │      │
│  │       last_name: string,                         │      │
│  │       email: string,                             │      │
│  │       company: string,                           │      │
│  │       personalized_message: string,              │      │
│  │       enrichment_status: 'enriched',             │      │
│  │       perplexity_research: string,               │      │
│  │       ...other fields                            │      │
│  │     }                                            │      │
│  │   ]                                              │      │
│  │ created_at: timestamp                            │      │
│  │ updated_at: timestamp                            │      │
│  └──────────────────────────────────────────────────┘      │
│                                                              │
│  ┌──────────────────────────────────────────────────┐      │
│  │           AGA Runs Progress                       │      │
│  ├──────────────────────────────────────────────────┤      │
│  │ run_id: uuid (PK)                                │      │
│  │ campaign_id: uuid (FK → campaigns.id)            │      │
│  │ status: enum('extracting', 'personalizing',      │      │
│  │              'completed', 'failed')              │      │
│  │ error_message: string (optional)                 │      │
│  │ updated_at: timestamp                            │      │
│  └──────────────────────────────────────────────────┘      │
│                                                              │
│  ┌──────────────────────────────────────────────────┐      │
│  │            Client Metrics                         │      │
│  ├──────────────────────────────────────────────────┤      │
│  │ user_auth_id: uuid (PK, FK → auth.users)        │      │
│  │ num_personalized_leads: integer                  │      │
│  │ hours_saved: float                               │      │
│  │ money_saved: integer                             │      │
│  └──────────────────────────────────────────────────┘      │
│                                                              │
└─────────────────────────────────────────────────────────────┘

RELATIONSHIPS:
• campaigns.user_id → auth.users.id (1:N)
• campaign_leads.campaign_id → campaigns.id (1:1)
• campaign_leads.user_id → auth.users.id (1:N)
• AGA Runs Progress.campaign_id → campaigns.id (1:N)
• Client Metrics.user_auth_id → auth.users.id (1:1)
```

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                           │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────────────────────────────────────────┐      │
│  │  Layer 1: Environment Variable Validation        │      │
│  │  • Zod schema validation at startup              │      │
│  │  • All required keys must be present             │      │
│  │  • Type checking for all env vars                │      │
│  └──────────────────────────────────────────────────┘      │
│                                                              │
│  ┌──────────────────────────────────────────────────┐      │
│  │  Layer 2: CORS Configuration                     │      │
│  │  • Whitelist trusted origins only                │      │
│  │  • Credentials enabled for auth                  │      │
│  │  • Method restrictions (GET, POST only)          │      │
│  └──────────────────────────────────────────────────┘      │
│                                                              │
│  ┌──────────────────────────────────────────────────┐      │
│  │  Layer 3: Input Validation                       │      │
│  │  • Request schema validation                     │      │
│  │  • File type validation (CSV only)               │      │
│  │  • File size limits (10MB max)                   │      │
│  │  • SQL injection prevention                      │      │
│  └──────────────────────────────────────────────────┘      │
│                                                              │
│  ┌──────────────────────────────────────────────────┐      │
│  │  Layer 4: Database Security                      │      │
│  │  • Service role key (not anon key)               │      │
│  │  • Row Level Security (RLS) in Supabase          │      │
│  │  • Parameterized queries                         │      │
│  │  • User ID verification                          │      │
│  └──────────────────────────────────────────────────┘      │
│                                                              │
│  ┌──────────────────────────────────────────────────┐      │
│  │  Layer 5: API Key Protection                     │      │
│  │  • Environment variables (never hardcoded)       │      │
│  │  • API keys never exposed to frontend            │      │
│  │  • Separate keys for dev/prod                    │      │
│  └──────────────────────────────────────────────────┘      │
│                                                              │
│  ┌──────────────────────────────────────────────────┐      │
│  │  Layer 6: Error Handling                         │      │
│  │  • Sanitized error messages                      │      │
│  │  • No stack traces in production                 │      │
│  │  • Detailed logging (server-side only)           │      │
│  └──────────────────────────────────────────────────┘      │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## ⚡ Performance Optimization Strategy

```
┌─────────────────────────────────────────────────────────────┐
│              PERFORMANCE OPTIMIZATIONS                       │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  1. BATCH PROCESSING                                        │
│     • Split leads into configurable batches (default: 10)   │
│     • Process batches sequentially                          │
│     • Process leads within batch in parallel                │
│     • Prevents memory overflow on large files               │
│                                                              │
│  2. PARALLEL AI CALLS                                       │
│     • Promise.allSettled() for batch processing             │
│     • Continues even if individual leads fail               │
│     • Maximizes throughput                                  │
│                                                              │
│  3. EARLY RETURN                                            │
│     • 202 Accepted response immediately                     │
│     • Processing happens asynchronously                     │
│     • User doesn't wait for completion                      │
│                                                              │
│  4. DEDUPLICATION                                           │
│     • Check database before processing                      │
│     • Skip already processed leads                          │
│     • Saves API costs and time                              │
│                                                              │
│  5. RETRY LOGIC                                             │
│     • 3 attempts with exponential backoff                   │
│     • 2 second initial delay                                │
│     • Handles transient API errors                          │
│                                                              │
│  6. DATABASE OPTIMIZATION                                   │
│     • Batch writes where possible                           │
│     • Single query for lead appending                       │
│     • Efficient JSON operations                             │
│                                                              │
│  7. STREAMING (Future)                                      │
│     • WebSocket for real-time status updates                │
│     • Reduces polling overhead                              │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## 📊 Data Transformation Pipeline

```
CSV INPUT
   ↓
┌──────────────────────────────────────────────────┐
│ STEP 1: PARSE                                    │
│ • csv-parse library                              │
│ • Convert to array of objects                    │
│ • Handle BOM, encoding                           │
└─────────────────┬────────────────────────────────┘
                  ↓
┌──────────────────────────────────────────────────┐
│ STEP 2: NORMALIZE                                │
│ • Map column names to standard format            │
│ • Handle variations:                             │
│   - "First Name" → first_name                    │
│   - "firstName" → first_name                     │
│   - "Email" → email                              │
│   - etc.                                         │
└─────────────────┬────────────────────────────────┘
                  ↓
┌──────────────────────────────────────────────────┐
│ STEP 3: VALIDATE                                 │
│ • Check required fields:                         │
│   ✓ email (must contain @)                       │
│   ✓ company (non-empty)                          │
│ • Filter out invalid leads                       │
└─────────────────┬────────────────────────────────┘
                  ↓
┌──────────────────────────────────────────────────┐
│ STEP 4: DEDUPLICATE                              │
│ • Group by email (case-insensitive)              │
│ • Keep first occurrence                          │
│ • Remove duplicates                              │
└─────────────────┬────────────────────────────────┘
                  ↓
┌──────────────────────────────────────────────────┐
│ STEP 5: ENRICH (PERPLEXITY)                      │
│ • Research company                               │
│ • Gather insights                                │
│ • Add research to lead object                    │
└─────────────────┬────────────────────────────────┘
                  ↓
┌──────────────────────────────────────────────────┐
│ STEP 6: PERSONALIZE (CLAUDE)                     │
│ • Generate personalized message                  │
│ • Validate output (max 25 words)                 │
│ • Add to lead object                             │
└─────────────────┬────────────────────────────────┘
                  ↓
┌──────────────────────────────────────────────────┐
│ STEP 7: SAVE                                     │
│ • Append to campaign_leads.lead_data             │
│ • Update campaign.completed_count                │
│ • Update Client Metrics KPIs                     │
└─────────────────┬────────────────────────────────┘
                  ↓
ENRICHED LEADS IN DATABASE
```

## 🔄 Error Handling & Recovery

```
┌─────────────────────────────────────────────────────────────┐
│                    ERROR HANDLING FLOW                       │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ANY ERROR OCCURS                                           │
│       ↓                                                      │
│  ┌────────────────────────────────────┐                     │
│  │ Is it retryable?                   │                     │
│  │ (Network, timeout, rate limit)     │                     │
│  └────┬───────────────────────┬───────┘                     │
│       │ YES                   │ NO                          │
│       ↓                       ↓                             │
│  ┌────────────┐         ┌─────────────┐                    │
│  │ RETRY LOGIC│         │ LOG ERROR   │                    │
│  │ • Attempt 1│         │ • Stack trace│                    │
│  │ • Wait 2s  │         │ • Context   │                    │
│  │ • Attempt 2│         │ • Lead email│                    │
│  │ • Wait 2s  │         └──────┬──────┘                    │
│  │ • Attempt 3│                │                            │
│  └────┬───────┘                │                            │
│       │                        │                            │
│       ├─ Success ──────────────┤                            │
│       │                        │                            │
│       └─ All Failed ───────────┤                            │
│                                ↓                            │
│                       ┌────────────────┐                    │
│                       │ GRACEFUL FAIL  │                    │
│                       │ • Mark lead as │                    │
│                       │   "failed"     │                    │
│                       │ • Continue with│                    │
│                       │   next lead    │                    │
│                       │ • Don't crash  │                    │
│                       │   entire batch │                    │
│                       └────────┬───────┘                    │
│                                │                            │
│                                ↓                            │
│                    ┌───────────────────────┐               │
│                    │ UPDATE RUN STATUS     │               │
│                    │ • If all failed:      │               │
│                    │   status = "failed"   │               │
│                    │ • If some succeeded:  │               │
│                    │   status = "completed"│               │
│                    │ • Log error_message   │               │
│                    └───────────────────────┘               │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## 🚀 Deployment Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    PRODUCTION DEPLOYMENT                     │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │            CDN (Cloudflare / Vercel)               │    │
│  │         Serves static frontend files                │    │
│  └───────────────────┬────────────────────────────────┘    │
│                      │                                      │
│  ┌───────────────────▼────────────────────────────────┐    │
│  │            React Frontend                           │    │
│  │   Deployed on Vercel/Netlify/Cloudflare Pages      │    │
│  └───────────────────┬────────────────────────────────┘    │
│                      │                                      │
│                      │ HTTPS                                │
│                      │                                      │
│  ┌───────────────────▼────────────────────────────────┐    │
│  │        Load Balancer (optional)                    │    │
│  │         Railway / Render / AWS                      │    │
│  └───────────────────┬────────────────────────────────┘    │
│                      │                                      │
│  ┌───────────────────▼────────────────────────────────┐    │
│  │         Backend API Servers                        │    │
│  │   • Express + TypeScript                           │    │
│  │   • Auto-scaling enabled                           │    │
│  │   • Health checks configured                       │    │
│  │   • Environment variables secure                   │    │
│  └─┬──────────────┬──────────────┬─────────────────────┘    │
│    │              │              │                          │
│    │              │              │                          │
│  ┌─▼──────┐  ┌───▼────┐  ┌──────▼────┐                    │
│  │Supabase│  │Perplex-│  │OpenRouter │                    │
│  │  DB    │  │ity AI  │  │ (Claude)  │                    │
│  │        │  │        │  │           │                    │
│  │• RLS   │  │• API   │  │• API      │                    │
│  │• Backup│  │  key   │  │  key      │                    │
│  └────────┘  └────────┘  └───────────┘                    │
│                                                              │
└─────────────────────────────────────────────────────────────┘

DEPLOYMENT PLATFORMS:
• Frontend: Vercel, Netlify, Cloudflare Pages
• Backend: Railway, Render, DigitalOcean, AWS, GCP
• Database: Supabase (managed PostgreSQL)
• Monitoring: Datadog, New Relic, Sentry
```

---

This architecture provides:
- ✅ Scalability (horizontal scaling possible)
- ✅ Reliability (retry logic, error handling)
- ✅ Performance (batch processing, parallel calls)
- ✅ Security (multiple layers of protection)
- ✅ Maintainability (clean separation of concerns)
- ✅ Observability (comprehensive logging)
